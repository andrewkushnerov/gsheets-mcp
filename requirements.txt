fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6
pydantic-settings>=2.2
google-api-python-client>=2.120
google-auth>=2.28
google-auth-oauthlib>=1.2

# Test-only, but kept in the same file: it is a handful of small pure-Python
# packages, and this way `python -m pytest tests` works straight after the
# install above instead of needing a second file and a second step.
pytest>=8.0
httpx>=0.27
